name: test-callee
on:
  workflow_call:
    inputs:
      push-image:
        type: boolean
        default: false
jobs:
  image:
    if: inputs.push-image
    runs-on: [self-hosted, linux, x64, tainan-node05]
    steps:
      - run: echo "would push"
